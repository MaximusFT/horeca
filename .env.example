# The demo stays in free deterministic local-agent mode unless this is explicitly enabled.
AGENT_MODE=local

# Optional live Responses API mode (server-side only):
# AGENT_MODE=openai
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-5.4-mini
